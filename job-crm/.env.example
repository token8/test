# Copy to job-crm/.env (git-ignored, manifesto §11) and fill in. Rotate the key quarterly.
TYPESAFE_API_KEY=
# JEV_MODEL=jev-1.13.0            # pin for a release; default jev-latest
# LAYA_URL=http://127.0.0.1:8771  # Laya sidecar
# DECISION_REMOTE=0               # kill switch: nothing leaves the machine
# DECISION_LOG_DIR=logs
